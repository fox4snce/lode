# Core dependencies
Flask==3.0.0
openai>=1.0.0
tiktoken>=0.5.0
pydantic>=2.0.0
numpy>=1.20.0

# Web framework and desktop
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pywebview>=4.4.0
requests>=2.31.0
python-multipart>=0.0.6

# Embeddings and ML (for offline embeddings)
onnxruntime>=1.15.0
tokenizers>=0.13.0

# Optional: For exporting models to ONNX (tools/export_embedder_onnx.py)
# transformers>=4.30.0
# optimum[onnxruntime]>=1.12.0

# NLP (for entity extraction)
stanza>=1.5.0

